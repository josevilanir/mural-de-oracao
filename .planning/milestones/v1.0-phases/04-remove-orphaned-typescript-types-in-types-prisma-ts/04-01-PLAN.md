---
wave: 1
depends_on: []
files_modified:
  - types/prisma.ts
autonomous: true
---

# Plan 04-01: Remove Orphaned TypeScript Types

## Objective
Remove the `Role` and `NotificationType` types from `types/prisma.ts` which are no longer consumed by any feature. This reduces maintenance noise and false confidence in type coverage.

## Tasks

<task>
<action>
Remove the `Role` export from `types/prisma.ts`.
</action>
<read_first>
- `types/prisma.ts`
</read_first>
<acceptance_criteria>
- `grep "export type Role" types/prisma.ts` or `grep "export enum Role" types/prisma.ts` returns no matches.
</acceptance_criteria>
</task>

<task>
<action>
Remove the `NotificationType` export from `types/prisma.ts`.
</action>
<read_first>
- `types/prisma.ts`
</read_first>
<acceptance_criteria>
- `grep "export type NotificationType" types/prisma.ts` or `grep "export enum NotificationType" types/prisma.ts` returns no matches.
</acceptance_criteria>
</task>

## Verification

### Must Haves
- [ ] `Role` is completely removed from `types/prisma.ts`.
- [ ] `NotificationType` is completely removed from `types/prisma.ts`.
- [ ] The TypeScript compiler does not complain about missing types (`npx tsc --noEmit` exits 0).
- [ ] Running `npx ts-prune | grep "^types/prisma.ts"` returns no output.

### Verification Steps
1. Run `npx tsc --noEmit` to ensure the build is not broken.
2. Run `npx ts-prune | grep "^types/prisma.ts"` to ensure these types no longer appear as orphaned, and no new orphans were introduced here.
